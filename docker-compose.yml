services:
  db:
    image: postgres:17
    environment:
      POSTGRES_USER: grove
      POSTGRES_PASSWORD: grove_dev
      POSTGRES_DB: grove
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data:
